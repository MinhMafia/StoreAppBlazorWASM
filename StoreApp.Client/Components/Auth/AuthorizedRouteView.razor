@* Components/Auth/AuthorizedRouteView.razor *@
@using Microsoft.AspNetCore.Components.Rendering
@using Blazored.LocalStorage
@inject ILocalStorageService LocalStorage
@inject NavigationManager Nav

@if (isChecked)
{
    if (isAuthorized)
    {
        <RouteView RouteData="@RouteData" DefaultLayout="@DefaultLayout" />
    }
}
else
{
    <div class="min-vh-100 d-flex justify-content-center align-items-center bg-light">
        <div class="text-center">
            <div class="spinner-border text-primary mb-3" style="width: 3rem; height: 3rem;" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="text-muted">Đang tải...</p>
        </div>
    </div>
}

@code {
    [Parameter]
    public RouteData RouteData { get; set; } = default!;

    [Parameter]
    public Type? DefaultLayout { get; set; }

    private bool isChecked = false;
    private bool isAuthorized = false;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await CheckAuthorizationAsync();
        }
    }

    private async Task CheckAuthorizationAsync()
    {
        var path = Nav.ToBaseRelativePath(Nav.Uri).ToLower();

        // Các trang công khai không cần auth
        var publicPages = new[] { "login", "admin/login", "register", "store", "store/products" };

        if (publicPages.Any(p => path.StartsWith(p)))
        {
            try
            {
                var token = await LocalStorage.GetItemAsStringAsync("authToken");
                if (!string.IsNullOrEmpty(token))
                {
                    var role = await LocalStorage.GetItemAsStringAsync("userRole");

                    if (path.StartsWith("admin/login"))
                    {
                        if (role == "admin" || role == "staff")
                        {
                            Nav.NavigateTo("/admin/home", forceLoad: true);
                            return;
                        }
                    }
                    else if (path.StartsWith("login") && !path.StartsWith("admin/login"))
                    {
                        if (role == "customer")
                        {
                            Nav.NavigateTo("/store", forceLoad: true);
                            return;
                        }
                        else if (role == "admin" || role == "staff")
                        {
                            Nav.NavigateTo("/admin/home", forceLoad: true);
                            return;
                        }
                    }
                }
            }
            catch { }

            isAuthorized = true;
            isChecked = true;
            StateHasChanged();
            return;
        }

        // Các trang khác cần auth
        try
        {
            var token = await LocalStorage.GetItemAsStringAsync("authToken");
            var role = await LocalStorage.GetItemAsStringAsync("userRole");

            if (path.StartsWith("admin"))
            {
                if (string.IsNullOrEmpty(token) || (role != "admin" && role != "staff"))
                {
                    Nav.NavigateTo("/admin/login", forceLoad: true);
                    return;
                }
            }
            else if (path.StartsWith("store"))
            {
                var isPublicStore = path == "store" || path.StartsWith("store/products");
                if (isPublicStore && string.IsNullOrEmpty(token))
                {
                    isAuthorized = true;
                    isChecked = true;
                    StateHasChanged();
                    return;
                }

                if (string.IsNullOrEmpty(token) || role != "customer")
                {
                    Nav.NavigateTo("/login", forceLoad: true);
                    return;
                }
            }
            else if (string.IsNullOrEmpty(path) || path == "/")
            {
                if (string.IsNullOrEmpty(token))
                {
                    Nav.NavigateTo("/store", forceLoad: true);
                    return;
                }

                if (role == "admin" || role == "staff")
                {
                    Nav.NavigateTo("/admin/home", forceLoad: true);
                }
                else
                {
                    Nav.NavigateTo("/store", forceLoad: true);
                }
                return;
            }

            isAuthorized = true;
        }
        catch
        {
            Nav.NavigateTo("/login", forceLoad: true);
            return;
        }
        finally
        {
            isChecked = true;
            StateHasChanged();
        }
    }
}
